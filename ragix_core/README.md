# RAGIX Core

This directory contains the core logic, components, and libraries that power the RAGIX platform. It is the heart of the application, containing everything from agent definitions to the AST analysis engine.