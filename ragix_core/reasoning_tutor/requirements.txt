# =============================================================================
# Reasoning Tutor Requirements
# =============================================================================
#
# Author: Olivier Vitrac, PhD, HDR | olivier.vitrac@adservio.fr | Adservio
# Date: 2025-12-24
#
# These are ADDITIONAL dependencies for the reasoning_tutor module.
# The main RAGIX requirements.txt must be installed first.
#
# Install (core + visualization):
#   pip install -r ragix_core/reasoning_tutor/requirements.txt
#
# Install with DSPy (optional, for offline policy compilation):
#   pip install -r ragix_core/reasoning_tutor/requirements.txt
#   pip install dspy-ai
#
# IMPORTANT: The Tutor works WITHOUT DSPy. DSPy is only needed for the
# optional TutorEnvelope Layer 3 (offline policy compilation).
# All previous production scripts continue to work unchanged.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Core Analysis Dependencies
# -----------------------------------------------------------------------------

# Numerical computing (semantic similarity, embeddings)
numpy>=1.24.0

# Data manipulation (feature matrices, results analysis)
pandas>=2.0.0

# -----------------------------------------------------------------------------
# Visualization (for analysis/visualize.py)
# -----------------------------------------------------------------------------

# Plotting (PCA plots, heatmaps, charts)
matplotlib>=3.7.0

# Scientific computing (dendrograms, hierarchical clustering, distance metrics)
scipy>=1.11.0

# Machine learning (PCA, StandardScaler, dimensionality reduction)
scikit-learn>=1.3.0

# -----------------------------------------------------------------------------
# Already in main requirements.txt (listed for completeness)
# -----------------------------------------------------------------------------

# requests>=2.31.0      # HTTP client for Ollama API
# pyyaml>=6.0           # YAML benchmark definitions
# pytest>=7.0.0         # Unit tests

# -----------------------------------------------------------------------------
# Optional: DSPy Integration (for TutorEnvelope Layer 3)
# -----------------------------------------------------------------------------
# DSPy is ONLY needed for offline policy compilation (the "Lawyer").
# Runtime execution (the "Law") NEVER requires DSPy.
#
# The "Law vs. Lawyer" model:
#   - Law (TutorV1): Deterministic verification, CHECK protocol, scoring
#   - Lawyer (DSPy): Optional optimizer that compiles policy bundles offline
#
# Install separately (not listed as hard dependency):
#   pip install dspy-ai>=2.5.0
#
# Why DSPy is optional:
#   1. Preserves Tutor runtime as dependency-free
#   2. Maintains deterministic behavior (DSPy runs offline only)
#   3. Allows paper reproduction without DSPy installed
#   4. Previous production scripts continue to work unchanged
#
# See REVIEW_extensions.md for the full integration specification.
# -----------------------------------------------------------------------------
