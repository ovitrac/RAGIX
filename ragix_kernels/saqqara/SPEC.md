# saqqara — specification

**Author:** Olivier Vitrac, PhD, HDR | olivier.vitrac@adservio.fr | Adservio | 2026-08-27
**Status:** frozen for P0. Interfaces freeze at the end of P1 (model) and P3 (analyzers).

Every function in this package must be justified by a proposition below. A function that no
proposition justifies does not belong here; a proposition that no test exercises is not a
specification, it is a wish.

## How to read this

Each proposition is falsifiable and carries the fixture that exercises it. Fixtures are
**generated by code** (`tests/saqqara/generators.py`) — no document is ever committed to this
repository, so every fixture's text is known to come from the generator. That positive property
replaces any list of strings a fixture must avoid: there is nothing to enumerate when the origin
of every character is provable.

Gates are numbered K1–K4, K6 and K7 by layer, and the tests that carry them live in `tests/saqqara/` as
`test_k<N>_<subject>.py`. The numbering is this family's own; it does not follow any other
convention in this repository.

Phases: **P1** model · **P2** adapters · **P2'** pdf adapter · **P3** analyzers ·
**P3'** section analyzer · **P4** envelope.

### Reading a measurement

Two rules earned against this package's own corpus comparison, kept here because they decide what a
number is allowed to settle.

**A proposition about an emitted kind asserts on the tree, not on records.** Records are a reader's
contract; a layer that produces them and no nodes has produced nothing. Seven green tests once
certified a layer that emitted no node at all, and the builder had been counting the loss under
`unmapped-observation` throughout. *A drop nobody reads is a silent drop.*

**Every classification ends in an `else` that counts.** A branch structure over resources, objects
or reasons with no terminal counter is a defect by construction: whatever it fails to recognise
falls through it in silence, and silence is indistinguishable from absence. Three placements were
lost that way in this package — an office record no build plan mapped, a form the resource walk
matched in neither branch, and, inside the fix for the second, a reference that resolved to nothing
and so had no subtype to match at all. The vocabulary test must read the fixture that produces the
terminal reason, or the `else` is decoration.

**Mass decides what to fix first; it does not decide what is true.** Quantities in real corpora
concentrate — a handful of documents routinely carry most of any total — so a uniform sample
measures the median document and answers a question nobody asked. But the converse error is
symmetrical: a hypothesis refuted **on aggregate** is refuted *for the aggregate*, and must be
re-asked wherever the aggregate is not the question. One hypothesis here was recorded refuted at 1%
of a corpus-wide total while being 100% of the cause in the four documents that actually carried the
defect.

---

## K1 — model: nodes, provenance, serialisation

Phase P1. Freezes the public interface of `model.py` and the signature part of `views.py`.

Every proposition here is exercised on `trees_per_format`: trees built directly through the model
API, one per format, each pinning its own locator convention. That is deliberate. A claim about the
model must not depend on a reader — testing "a node cannot exist without provenance" against a
parsed spreadsheet would prove as much about the spreadsheet reader as about the model, and would
leave the model untested until that reader exists. Readers are proved at K2, on their own fixtures.

K1.9 is the one exception, and deliberately so: it is a claim about the addresses a reader actually
issues, and those do not exist until a reader has issued them. It is exercised on read documents.

**This interface was reopened once.** On 2026-08-27, after the builder was written, `PptxLocator`
gained a `notes` field: a slide and its speaker notes shared a single coordinate, so a locator was
not a unique address inside its own format. That is a defect in what this package promises — a
citation that cannot say whether it points at a slide or at what the speaker said about it. It cost
one field before publication; after publication it would have been a breaking change. K1.9 was
added in the same edit so the property is now asserted rather than assumed.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K1.1 | A node cannot be constructed without provenance: source, format, locator chain, producing kernel and kernel version. | `trees_per_format` | any construction path yielding a node whose provenance is absent or empty |
| K1.2 | A tree survives a JSON round-trip byte-identically: re-serialising a parsed serialisation reproduces the original bytes. | `trees_per_format` | one differing byte |
| K1.3 | A kind outside the registry is refused at construction; a registered kind is accepted without touching the core. | `trees_per_format` | an unregistered kind accepted, or a newly registered kind rejected |
| K1.4 | An inferred node carries a confidence below 1.0 and the name of the kernel that inferred it; a node read directly from the file carries 1.0. | `trees_per_format` | an inferred node at 1.0, or a read node below it, or a node without its producing kernel |
| K1.5 | The structure signature is invariant under serialisation: the signature of a tree equals the signature of its round-trip. | `trees_per_format` | two differing signatures |
| K1.6 | The signature reconciles with its own tree: kind totals sum to the node count, and outline entries sum to the per-level heading counts before any cap. | `trees_per_format` | a signature disagreeing with the tree it describes |
| K1.7 | The same content read from two different paths yields the same core signature. | `trees_per_format` | a signature that varies with the file path |
| K1.8 | The signature separates structured mass from flat mass, and stays meaningful for both a spreadsheet and a flat word-processing tree. | `trees_per_format` | a flat tree and a structured tree scoring alike |
| K1.9 | Within one document and one format, a locator chain is a unique address for a node: no two nodes of the tree **the builder produces** serialise to the same coordinate. Analyzers add derived nodes later, and a derived node legitimately cites the position it was derived from — it is told apart by naming its own producer, not by holding a different address. Several observations may share a position — a cell and its own border — but only where they become one node. | `slide_deck`, `mixed_workbook`, `docx_two_tier`, `markdown_document` | two nodes of one document sharing a coordinate |
| K1.10 | A tree remains serialisable after any analyzer in this package has run over it: `to_json` writes it and the round trip is byte-identical. A fact that is neither JSON-native nor `to_dict`-aware is refused at serialisation, never stringified. | `pdf_caption_below` | an analyzer leaving a tree `to_json` cannot write, a round trip that is not byte-identical, or a fact stored as its repr |

## K2 — adapters: raw facts, one reader per format

Phase P2; the pdf reader in P2'. An adapter emits facts; it never interprets them.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K2.1 | Every spreadsheet cell record carries exactly the declared fact set — data type, boldness, number format, lock flag, formula, merge flag — and its geometric keys live in the locator, not among the facts. Nothing more, nothing less. | `numeric_bold_header` | one fact missing, one fact present that the declared set does not contain, or a geometric key smuggled in as a fact |
| K2.2 | The facts are raw reads, never interpretation: a numeric bold header records both its numeric type and its boldness, and neither fact alone disqualifies it as a header. | `numeric_bold_header` | a fact rewritten by the adapter, or a header discarded at read time |
| K2.3 | A merged region records its facts at the anchor; continuation cells invent none. | `two_tier_header` | a continuation carrying invented facts |
| K2.4 | Every emitted fact is a JSON-serialisable value built from primitives, lists of primitives, and mappings of them; no library object reaches the output. | `numeric_bold_header`, `mixed_workbook`, `markdown_document` | a value that fails to serialise |
| K2.5 | The adapter declares its version, and the version changes whenever the emitted fact set changes. | `mixed_workbook` | a fact-set change shipped under an unchanged version |
| K2.6 | The word-processing adapter emits, per table, the resolved grid and per-cell facts — span, vertical merge, text, emptiness, fillability, bold, shading — plus the locator, including tables that live in the page-header flow and tables nested inside a cell. | `docx_header_stream`, `docx_nested` | a table in either flow absent from the output |
| K2.7 | A nested table does not bleed into the text of its parent cell. | `docx_nested` | parent cell text containing the nested table's content |
| K2.8 | A fillable marker inside a cell and a marker outside any table are distinguished and typed separately. | `docx_markers` | the two conflated |
| K2.9 | Slot arithmetic over a blank/filled pair holds — blanks in the blank form minus blanks in the filled form equals the answers — with vertical-merge continuations excluded from the count. | `docx_twin_pair` | continuations counted as blanks, or the arithmetic failing to balance |
| K2.10 | A table's index is counted within its own location flow — body, page header, nested — never globally. | `docx_header_stream` | an index continuing across flows |
| K2.11 | An unsupported or unreadable file fails closed: a typed, counted refusal, never an empty tree. | `unsupported_format` | an empty tree returned as success |
| K2.12 | Byte-identical duplicates are dropped by content hash, and the drop is counted. | `duplicate_pair` | a silent drop, or a duplicate counted as independent |
| K2.13 | Where a document declares its own outline, the adapter reproduces it exactly: one heading per declared entry. | `pdf_outline` | a count that differs from the declared outline |
| K2.14 | A page with no text layer is routed to a declared pending state and is never silently emptied. | `pdf_no_text_layer` | a page reported as empty content rather than as pending |
| K2.15 | The presentation reader emits one record per slide and per shape, numbering slides as a reader counts them, and keeps speaker notes distinct from what is shown on the slide. | `slide_deck` | a zero-based slide number, or notes merged into slide text |
| K2.16 | The markdown reader emits one record per block with the line it starts on, and front matter is carried as document metadata rather than as body text. | `markdown_document` | front matter read as a paragraph, or a block without its line |
| K2.17 | A cell whose value is empty or only whitespace is emitted as blank, and is an addressable slot where it is ruled or anchors a merge. Its data type still records that the file held a string, so an empty string and an absent value stay distinguishable. | `empty_string_cells` | an empty string counted as a value, or the distinction from an absent value lost |
| K2.18 | A table on a slide is read as a grid, in the same fact vocabulary a spreadsheet or word-processing grid uses, and its cells are addressable individually. A shape carrying a table is never passed over for having no text frame. | `twin_grid_pptx`, `slide_deck` | a slide table absent from the output, or its cells emitted in a vocabulary the grid core cannot read |
| K2.19 | Every reader declares its fact vocabulary **per record kind**, not as one flat set for the whole reader: a cell and a paragraph are not described by the same facts, and a kind emitted with no declaration is a failure rather than a default. | `mixed_workbook`, `docx_markers`, `slide_deck`, `pdf_outline`, `markdown_document` | a record whose kind no declaration covers, or a fact outside the vocabulary declared for its kind |
| K2.20 | The declarations are checked against what the readers emit **in both directions**: a declared name that no reader produces on any fixture is a defect, not a reserve held for later. | `mixed_workbook`, `docx_markers`, `slide_deck`, `pdf_outline`, `markdown_document` | a declared name never emitted, or an emitted fact never declared |
| K2.21 | Where a record kind takes its fact names from the document rather than from the reader, the vocabulary is declared **open**, with the names the reader reserves for itself listed; open is a declaration, not the absence of one. | `markdown_document` | an open vocabulary declared as a closed set, a closed one declared open, or a reserved name the declaration does not list |
| K2.22 | The grid kinds — a table and its cells — carry **one** vocabulary shared by the word-processing and the presentation reader, held in one place rather than agreed by two copies, so the grid core reads a contract and not a coincidence. | `twin_grid_docx`, `twin_grid_pptx` | the two readers declaring different vocabularies for the same grid kind |
| K2.23 | A paragraph's weight is emitted as a **fraction of its characters**, never as a flag, and its size is emitted both absolutely and against the document's own modal size. A boolean cannot separate a heading set entirely in bold from a sentence with one word emphasised, and a rule about dominance written over a flag is a rule that cannot hold. | `format_headings_docx` | a boolean weight fact, a fraction taken over runs rather than characters, or a size ratio against anything but the document's modal size |
| K2.24 | The size a reader reports is the size **on the page**, not the operand that asked for it: a document may set its type through the size operand, through the text matrix, or through the page transformation, and only their product is what a reader sees. The reported size is the operand scaled by the length of the vertical column of each matrix, so that rotated text reads at the size it is set in rather than at zero. | `pdf_type_scales` | the same type read at different sizes depending on which of the three carries the scale, or rotated text reading as zero |
| K2.25 | A run of type reports the **width it occupies on the page**, measured from the font's own `/Widths` and scaled by the matrices that placed it — the K2.24 distinction along the other axis. Where the font declares no widths, or declares none for a character in the run, the width is **unknown**: a plausible number here would be indistinguishable from a measured one everywhere downstream, and an extent is the one fact a layout rule cannot safely estimate. | `pdf_text_widths` | a width estimated from character count, one that ignores the text or graphics matrix, or a run with an unmeasurable font reporting a number |

## K3 — analyzers: tables, header bands, islands, tree services

Phase P3; the section analyzer in P3'. This is where abstention is proved.

### K3.a — segmentation and typing

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.1 | The segmentation cascade recovers block boundaries exactly on a multi-object sheet; a declared table object is an authoritative boundary. | `mixed_workbook` | one boundary off by a row or a column |
| K3.2 | A blank cell inside a bordered box never splits it, and a fully blank spacer row is bridged. | `mixed_workbook` | a box split by its own interior blank |
| K3.3 | A blank slot remains addressable and does not split its block. | `mixed_workbook` | a slot dropped or a block split at it |
| K3.4 | An empty bordered box is dropped with a trace: counted, described, never silent. | `mixed_workbook` | a drop absent from the trace |
| K3.5 | The cascade beats the one-block-per-sheet baseline, and the comparison is asserted rather than assumed. | `mixed_workbook` | the baseline matching or beating the cascade |
| K3.6 | Typing table / text / list is exact, including where merged value cells imitate text rows: a majority-merged block that is a table types as a table. | `mixed_workbook` | the merge count deciding the type |
| K3.7 | An ambiguous layout yields a typed abstention — reduced confidence, excluded from derived views — never a guess. | `ambiguous_layout` | a confident type on an undecidable block |

### K3.b — anchoring and navigation

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.8 | Every table cell anchors to its column header and to its row-label chain, with group-label carry-forward restricted to leading blank positions. | `mixed_workbook` | a carry-forward reaching an interior blank |
| K3.9 | Carry-forward beats the nearest-populated-cell-to-the-left baseline, and an interior data blank is never invented. | `mixed_workbook` | an invented interior value |
| K3.10 | Navigation returns each neighbour together with its parents, and skips the reference's own merged extent. | `mixed_workbook` | a neighbour returned without ancestry, or a cell returned as its own neighbour |
| K3.11 | Every block node carries the full locator; every cell locator adds the cell level, including the merged extent. | `mixed_workbook` | one locator field absent |
| K3.12 | The locator of a merged cell names its extent. | `mixed_workbook` | an extent absent or wrong |

### K3.c — header bands, laminar or abstain

The rules run in this order, and each names itself in the trace exactly as written here. A rule
that exists only in code is a rule this specification does not know about, and the next person to
read a trace has nowhere to look it up.

**Splitting a block** — `R1-laminarity` · `R2-uniform` · `R3-title` · `R4-style-contrast` ·
`R5-section-rows` · then the label zone · `R7` the core is what is left.

**Finding the label zone**, first rule that fires:

- `L1-band-header-span` — a merge on the first header row, starting at the block's left edge,
  declares the width of the label zone by spanning it.
- `L2-slot-column` — a column that is entirely addressable and empty is where the answers go;
  the textual columns to its left are the question. This rule is what makes a **blank** form
  readable: an unfilled form has no values anywhere, so a contrast between labels and values has
  nothing to compare, and the rule below it finds nothing.
- `L3-type-contrast` — a text column with values of another type somewhere to its right.
- `L4-no-label-evidence` — none of the above fired, and no label columns are claimed.

`R2-uniform` counts a slot as evidence. A block whose every value looks alike still has a shape if
part of it is addressable and waiting to be filled; abstaining there would throw away the only
structure such a block has.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.13 | The split into core, header band, label columns, title and section rows is exact across the merge-orientation taxonomy, by the ordered rules named above; the rule that fired is in the trace. | `two_tier_header`, `label_tiling`, `full_width_title`, `section_row`, `merged_answer_area`, `numeric_bold_header`, `headerless_list`, `totals_row_and_column` | one region boundary wrong on any case |
| K3.14 | A label tiling and its band header are recovered exactly, including nested tiles. | `label_tiling` | a tile merged, split, or misattributed |
| K3.15 | Two header merges that overlap without containment produce an abstention: no band is emitted. | `overlapping_merges` | a band emitted over a non-laminar layout |
| K3.16 | A block offering no discriminating evidence abstains rather than choosing. An addressable empty slot counts as evidence: a block that is partly waiting to be filled has a shape, and abstaining on it would discard the only structure it has. | `undecidable_block` | a band chosen without evidence, or an abstention on a block carrying slots |
| K3.17 | Every abstention reason belongs to a closed vocabulary, and an abstention without a reason is impossible. | `overlapping_merges`, `undecidable_block` | a reason outside the vocabulary, or an abstention with none |
| K3.18 | The trace is decomposed: each evidence component is named and readable on its own. No component is blended into a single score. | `two_tier_header` | a scalar score standing in for the components |
| K3.19 | The whole analysis result is JSON-serialisable. | `two_tier_header` | a value that fails to serialise |
| K3.20 | Against its baselines the method wins where it claims to win and ties elsewhere, and the ties are recorded rather than omitted. | `two_tier_header`, `section_row`, `label_tiling`, `numeric_bold_header`, `headerless_list` | an unrecorded tie, or a claimed win that measurement refutes |

### K3.d — header and label chains

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.21 | A column chain reads broadest first by interval containment; a vertical merge spanning two tiers contributes exactly one rung. | `two_tier_header` | a rung duplicated or ordered depth-first |
| K3.22 | An empty merged tile surfaces as an explicitly typed blank rung: a missing label is an addressable fact, not a hole. | `label_tiling` | a blank tile omitted from the chain |
| K3.23 | A reference inside the header band or the label zone raises: those regions are not value candidates. | `two_tier_header`, `label_tiling` | a header cell accepted as a value position |
| K3.24 | The exposed column header remains the deepest element of the chain, and is never the reference itself. | `two_tier_header` | the reference returned as its own header |
| K3.72 | Every rung carries the **address of the cell it was read from** beside its text, and the address resolves: following it lands on the cell holding that text. A merged tile reports its **whole extent**, so one tile is one rung and one range rather than a corner of itself; a **blank** rung is addressed like any other, since where a label is missing is the fact a reviewer most needs. An abstention returns no rungs rather than rungs pointing nowhere. The texts are a **view** of the rungs, derived in one place, so a chain and its addresses cannot disagree. The rung is a mapping of primitives: everything this projection returns is storable beside the answer it explains, and an object here would be the one value that fails to serialise — met by whoever stored a chain, not by whoever wrote it. | `two_tier_header`, `label_tiling`, `overlapping_merges` | a rung without an address, an address that resolves to another text, a merge reported as a corner, a blank rung unaddressed, an abstention carrying rungs, or a result that will not serialise |

### K3.e — islands

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.25 | A block holding two or more disconnected value islands records per-island cores and subtitles, reports an honestly empty column chain where there is no column header, and raises a segmentation-feedback flag. | `two_islands` | an invented column header, or islands fused into one core |
| K3.26 | The segmentation-feedback flag is recorded and never acted upon: the block is not re-segmented on the analyzer's own authority. | `two_islands` | an automatic re-segmentation |
| K3.27 | A non-laminar block stays uncertain, and the fallback is flagged in every anchoring result it produces. | `overlapping_merges` | a result that hides its fallback |
| K3.28 | The island fallback changes nothing outside its trigger: chains on all other cases are unaffected. | `two_tier_header`, `label_tiling`, `section_row`, `totals_row_and_column` | any chain changing when the fallback ships |

### K3.f — the same core across formats

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.29 | A word-processing table is typed layout / data-form / uncertain by ordered hard rules: positive evidence promotes, and its absence abstains. | `docx_layout_prose`, `docx_two_tier` | a promotion without evidence |
| K3.30 | The typing beats the every-table-is-data baseline. | `docx_layout_prose` | the baseline matching it |
| K3.31 | For a data-form table, the same grid core reproduces the spreadsheet semantics: core split under span and vertical merge, label tiling, chains, self-reference guard. One logical table written in three formats analyses identically. | `docx_two_tier`, `docx_label_tiling`, `twin_grid_xlsx`, `twin_grid_docx`, `twin_grid_pptx` | a divergence between the formats on the same shape |
| K3.32 | A table typed layout or uncertain is not analysed: abstention by typing, surfaced, never guessed. | `docx_layout_prose` | an analysis performed on a layout table |
| K3.33 | Detail columns are label-eligible under `L2-slot-column`: their cells are label-zone positions and they enrich the reconstructed question. | `docx_label_tiling` | a detail cell addressed as an answer |
| K3.34 | The grid core consumes a format-neutral fact vocabulary; each format adapter declares its mapping rules in order, and the mapping is recorded in the trace. | `mixed_workbook`, `docx_two_tier` | a format name reaching the core, or an undeclared mapping rule |

### K3.g — tree services

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.35 | The document-title cascade walks its rungs in declared order, metadata title outranking content title. | `trees_per_format` | a rung consulted out of order |
| K3.36 | The node a title came from keeps its provenance. | `trees_per_format` | a title without a citation |
| K3.37 | The page policy is per format and declared: exact pages where the format has them with skips counted, word windows where it does not, sheet index for spreadsheets, one-based slides for presentations. | `trees_per_format` | an undeclared policy, or an uncounted skip |
| K3.38 | Lookup accepts literal, alternation, case-insensitive, regular-expression and pre-compiled patterns, and honours the page restriction under every policy. | `trees_per_format` | a policy under which the restriction is ignored |
| K3.39 | The services hold through the real adapters, not only through hand-built trees: locator drift is caught here. | `docx_label_tiling`, `mixed_workbook` | a service that works on a built tree and fails on a read one |

### K3.j — the builder: facts into a tree

Phase P3, before any analyzer. The readers produce a stream of observations; the analyzers of
K3.a-K3.g speak of blocks, bands and tiles. Something has to turn one into the other, and it has
to be one thing: without it every analyzer re-invents the same cross-referencing, and they drift.

This is also where a document stops being a list and becomes a pyramid, which is the step the
whole package is named for.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.54 | Every observation reaches exactly one node, one attachment, or one counted drop — the three totals sum to the number of observations read. Nothing is lost and nothing is duplicated. | `mixed_workbook`, `docx_two_tier` | a total that does not reconcile, or one observation reaching two nodes |
| K3.55 | A node's provenance is derived from the observation's own locator and names the reader that made it; the builder never invents a coordinate it was not given. | `mixed_workbook` | a locator that appears in no observation, or a node citing the builder as the source of what it says |
| K3.56 | Border observations are matched to their cell by position and attached as facts of that node; a border matching no cell is a counted drop with its reason, never a silent discard and never a node of its own. | `mixed_workbook` | a border silently dropped, a border becoming a node, or an unmatched border passing unreported |
| K3.57 | Two builds from the same observations produce byte-identical trees. | `mixed_workbook`, `slide_deck` | one differing byte between two runs |
| K3.58 | Every format is built through one contract; what differs between formats is declared as data, not as a separate code path a caller has to know about. | `mixed_workbook`, `docx_two_tier`, `pdf_outline`, `slide_deck`, `markdown_document` | a format needing a caller to choose a different entry point |

### K3.h — sections

Phase P3'. A section name is the most useful structural fact a document offers and the least
reliably recorded one: some documents declare an outline, some style their headings, some number
them in the text and style nothing, and some do all three inconsistently. So the names are
collected per **channel** — one named route from a file feature to a candidate — and every
candidate then runs the same gauntlet.

The eight reader channels, closed: `pdf-numbered-heading` · `pdf-native-toc` · `docx-heading-style` ·
`docx-numbering-property` · `xlsx-sheet-name` · `xlsx-section-row` · `xlsx-block-title` ·
`pptx-slide-title`. One further channel, `outline-promotion`, is fed by an analyzer rather than a
reader and is kept apart from them for exactly that reason.

**Order.** `sections` runs last in the default chain: on a spreadsheet it reads block titles and
section rows the header analysis produced. `outline` (K3.i) runs **after** it, never before —
promoted first, every numbered line would already be a heading, and these channels would be
reading the package's own conclusions back to themselves.

Two failures are worth stating plainly, because both produce output that looks correct.

A **running header** is a title-shaped line that repeats on every page. Read as a section it
squats at the top of every ancestry in the document, and every answer beneath it is then filed
under a page number's neighbour. It is recognised by recurrence, not by position.

A **printed table of contents** is a page of title-shaped lines with page numbers attached. Read
as sections it produces a complete, plausible, entirely duplicated outline.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.40 | Every section record names the channel that produced it and carries its own trace. Eight channels are fed by readers and are closed; a channel fed by an analyzer is listed separately, so an inference is never filed among the observations. | `sections_multi_channel` | a record without a channel, a channel outside the lists, or an inferred heading reported under a reader's channel |
| K3.41 | A document that yields no section is reported as blind, with the reason — no text to read, no channel that applies, or every candidate rejected — never silently absent from the counts. | `sections_multi_channel`, `running_headers` | a document that produced nothing and appears nowhere |
| K3.42 | In a laid-out document, a heading whose number and text share a line is a candidate; a line holding a number alone joins the line beneath it, unless that line is itself a number, a printed-contents line, or a heading in its own right. | `sections_multi_channel` | a split heading left unjoined, or a number joined to a line that was already a heading |
| K3.43 | Printed table-of-contents lines — a title, dot leaders, a page number — are refused under their own reason and never read as sections. | `sections_multi_channel` | a contents page reproduced as an outline |
| K3.44 | A title recurring on three or more distinct pages is page furniture: dropped, counted, and absent from every ancestry. | `running_headers` | page furniture entering an ancestry, or a drop that is not counted |
| K3.45 | A candidate passes an ordered gauntlet of declared tests; every rejection is counted under a reason from a closed vocabulary, and the order the tests ran in is recorded. | `sections_multi_channel` | a rejection without a reason, a reason outside the vocabulary, or an unrecorded order |
| K3.46 | Each channel emits under its own name: sheet name, section row and block title for spreadsheets; heading style and numbering properties for documents; slide title for presentations. | `sections_multi_channel` | two channels reporting under one name, or a channel that never fires on a document built to trigger it |
| K3.47 | Every node carries the ordered section ancestry covering it, broadest first; a node under no section carries an empty chain rather than a guessed one. | `sections_multi_channel` | an invented ancestor, or an ancestry ordered deepest first |
| K3.48 | Against the baseline — every styled heading is a section, nothing filtered — the difference is measured and reported in both directions. | `sections_multi_channel`, `running_headers` | a comparison asserted without measurement, or one that reports only the wins |

### K3.i — typed outline

Phase P3'. Opt-in, and ordered after K3.h.

A promotion **adds** a node; it never rewrites the paragraph it came from. The paragraph is what a
reader observed and stays exactly that, while the promotion sits beneath it as a separate inferred
node, citing the position it came from and naming this analyzer — not the reader — as its
producer. So an observation is never silently replaced by an inference, and a caller who disagrees
with a promotion still has the document as it was read. A caller who wants promotions inside the
section ancestry runs `sections` again afterwards, and both passes appear in the trace.

A numbered line is not a heading because it is numbered. It is a heading when it takes its place
in a walk that the rest of the document also walks. These propositions are separated from K3.h
because they prove on **built trees**, where the label sequence is the whole input, while the
section channels of K3.h prove on **read documents**, where the channel is. Two natures of
fixture, two blocks.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.49 | A typed label promotes to a heading only where it takes a legal step in an outline walk; the same label outside such a walk stays a paragraph. | `numbered_outline_trees` | a label promoted on its shape alone |
| K3.50 | An enumeration is not an outline: a run of numbered items with no descent stays paragraphs. | `numbered_outline_trees` | an enumerated list promoted to headings |
| K3.51 | A walk too short to be evidence abstains, and a flat walk promotes only where a second signal corroborates it. | `numbered_outline_trees` | a two-item chain promoted, or a flat chain promoted uncorroborated |
| K3.52 | A restart at an annex yields a second walk rather than breaking the first, and a stray restart never captures the walk in progress. | `numbered_outline_trees` | two walks fused, or a stray label capturing the walk |
| K3.53 | Every label considered and not promoted is counted in the trace. | `numbered_outline_trees` | a rejection absent from the counts |

### K3.k — headings the document only shows

Phase P3''. Opt-in, and ordered after K3.h for the reason K3.i is: promoted first, this analyzer
would feed the section channels its own conclusions and read them back as corroboration.

Some documents carry no outline, no heading style and no numbering — nothing an outline is
*declared* with. They still have headings, and a reader sees them instantly, because they are set
in larger type than the text around them. That contrast is a fact the reader already records; what
is missing is the step that reads it. This block specifies that step for both kinds of
document: for laid-out ones, where the signal is size, and for word-processing ones that style
nothing, where it is weight.

The unit is a **line**, not a placement. A reader emits one observation per text-showing operation,
and a line of a laid-out document is on average two and a half of them, so a rule applied to
placements would cut a heading into fragments and count each as separate evidence. Line assembly is
therefore a declared step of this analyzer with its own trace, not a preparation hidden before it.

A promotion **adds** a node, exactly as in K3.i: the paragraph a reader observed stays what it was,
and the inference sits beneath it citing the position it came from.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K3.59 | Placements are assembled into lines before any rule is applied — same page, same vertical band, ordered by horizontal position — and the assembly is reported in the trace with the count it collapsed. | `format_headings_pdf` | a rule applied to a placement, or an assembly the trace does not report |
| K3.60 | Body size is the modal size by **character mass**, not by how many blocks carry it: a heading repeated on every page must not outweigh the text it heads. Sizes are binned before counting, and a tie is broken by the smaller size so that the choice is deterministic rather than an artefact of reading order. | `format_headings_pdf` | body taken by block count, an unbinned comparison, or a tie resolved by insertion order |
| K3.61 | Sizes within the declared gap are one visual tier: a document setting a heading at 15.0 and 15.5 has one heading tier, not two. Clustering never reaches down to the body mass. | `format_headings_pdf` | two tiers where a reader sees one, or a cluster chaining into the body size |
| K3.62 | A tier at or above the declared factor times body size is the title level; the remaining tiers are a ladder by descending size, each admitted only with a minimum number of heading-shaped lines behind it, so that a single decorative line does not become a level. Every tier refused is counted with its reason. | `format_headings_pdf` | a one-off promoted to a level, a ladder ordered other than by descending size, or a refusal absent from the counts |
| K3.63 | Promotion runs an **ordered** shape gauntlet, each rule naming itself in the trace: a line that is empty, too long, too many words, or ends in sentence punctuation is refused, and the first rule that refuses it is the one recorded. | `format_headings_pdf` | an unordered test, a refusal recorded against a later rule, or a line promoted on size alone |
| K3.64 | A document with no size contrast **abstains** — every line at one size yields no promotion at all, rather than a document promoted entire or a body size elected as a heading tier. | `no_format_contrast` | any promotion on a document of uniform size |
| K3.65 | Promotions are new inferred nodes under the `format-promotion` channel: `origin` inferred, confidence below 1, provenance citing the line's own position, producer named as this analyzer, and every pre-existing node byte-identical afterwards. | `format_headings_pdf` | a mutated node, a promotion at confidence 1, or a promotion whose provenance cites the document rather than the line |
| K3.66 | The channel is routed with the other derived channels, never among the eight a reader fills. | `format_headings_pdf` | `format-promotion` counted as a reader channel |
| K3.67 | Against the baseline — every line larger than the most common size is a heading — the difference is measured and reported **in both directions**, and every comparable fixture lands on one side or the other so that none drops quietly out of the comparison. | `format_headings_pdf`, `no_format_contrast` | a comparison asserted without measurement, one that reports only the wins, or a fixture in neither column |
| K3.68 | Two signals, tried in a **declared order**: size first, because sizes rank and can carry a ladder of levels; weight second and only where size found no contrast, because a document that has already ranked its headings must not be given a second, contradictory answer. The trace names the signal that decided. | `format_headings_pdf`, `format_headings_docx` | weight consulted where size decided, an undeclared order, or a trace that does not say which signal ran |
| K3.69 | A weight promotion is **flat**: bold does not rank, so it yields one declared level and never a ladder, at a confidence below the one size earns. | `format_headings_docx` | a weight promotion at more than one level, or at the same confidence as a size promotion |
| K3.70 | Weight is read as dominance, not as presence: a line is promoted only where the declared fraction of it is bold, so a paragraph carrying one emphasised word is refused. Against the boolean baseline — any bold run makes a heading — the difference is measured in both directions. | `format_headings_docx`, `no_weight_contrast` | a partly bold paragraph promoted, or a comparison against the boolean asserted without measurement |
| K3.71 | Where a document **declares** its own outline, this analyzer adds no heading inferred from its type. A declaration is what the document says about itself; an inference set beside it can only agree redundantly or disagree, and neither is worth the doubt it casts on the declaration. The skip is **named** and carries the number of lines it would otherwise have promoted, so a decision not to act is countable rather than invisible. | `pdf_declared_outline`, `format_headings_pdf` | a size-inferred heading beside a declared outline, a silent skip, a skip whose count is missing, or inference suppressed on a document that declares nothing |

## K4 — envelope

Phase P4. Two deliverables of this phase are preconditions of the first public push, not
finishing touches:

- the repository guard (`tools/check_forbidden.py`) must run **in continuous integration**, on
  push and on pull request. The `.git/hooks/pre-commit` launcher is local to one clone: it does
  not protect a second clone, a contributor, or anything arriving through a pull request. Until
  the CI check exists, the guard covers one machine and nothing else.
- the public `README.md` and this specification must carry no reference to the provenance of the
  behaviour they describe.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K4.1 | Two runs over the same input produce the same tree and the same root hash over that tree. | `mixed_workbook` | two differing roots |
| K4.2 | The root hash covers the content of the input, not its path: moving a file leaves the root unchanged, editing it changes the root. | `mixed_workbook` | a root that follows the path rather than the bytes |

**Note on measurement.** K4.1 is measured on the tree and its root hash, not on the kernel's
output file: the shared envelope stamps a wall-clock timestamp into every output, so two identical
runs never produce identical files. That is correct behaviour for the envelope and out of scope
for this family to change.

## K6 — objects: what a document shows rather than says

Phase P6. `figure` and `caption` have been registered kinds with no producer; this gate gives them
one, and adds `vector_region`.

The layer turns on the distinction the rest of this specification already carries. What a file
**contains** is an observation: the bytes of a picture are in the file, and saying so claims
nothing. What its parts **have to do with each other** is an inference: "the line beneath this
image is its caption" is a judgement about layout, and layout lies.

**A placement is the node, not the stored object.** An image drawn twice is two figures sharing one
asset — the file holds the bytes once and puts them on the page twice, and a tree reporting one node
would be describing the file's storage rather than its pages. The placement's box is the unit square
under the transformation that drew it, which is the same distinction K2.24 settles for type: the
operand says what was asked for, the matrix says what reached the page.

**No image bytes enter the tree.** A node carries the hash of its source and its provenance; the
bytes live in a side-car store addressed by that hash, so the JSON round-trip of K1 is unaffected —
a reference is a string like any other.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K6.1 | An image a file holds is emitted as an **observation**, one node per **placement**: `origin` read, confidence 1.0, `source` naming how it was held, and the coordinates of that placement. Extraction claims nothing about meaning. | `pdf_image_xobject` | an extracted figure below confidence 1, one emitted as inferred, or one node standing for two placements |
| K6.2 | **No image bytes enter the tree.** A node carries a hash and its provenance, and the canonical JSON round-trip is byte-identical to that of a tree carrying no assets at all. | `pdf_image_xobject` | a byte payload reachable from a node, or a round-trip that differs |
| K6.3 | One image drawn twice is **two nodes and one asset**: the store holds the bytes once, the manifest records both positions, and the two nodes differ in locator and in box. | `pdf_image_twice` | two assets stored, one node emitted, or a position the manifest does not record |
| K6.4 | A figure's box is the **unit square under the transformation that drew it**, never the stored image's pixel dimensions — the same distinction between what was asked for and what reached the page that K2.24 settles for type. | `pdf_image_twice` | a box taken from the stored width and height, or two placements of one image reporting the same box |
| K6.5 | An image carried inline in the content stream is a **named, counted skip** in this phase, and `source` therefore carries only the values something emits. A declared value nothing produces is the defect K2.20 exists to prevent. | `pdf_inline_image` | a silent skip, an uncounted one, or a declared source value never emitted |
| K6.6 | A node whose asset is absent from the store is a **counted, named failure** — never a node quietly without its picture. | `pdf_image_xobject` | a missing asset passing for success |
| K6.7 | **No object is dropped in silence.** Every image the reader declines — carried inline, naming an object that is not there, or decoding to nothing — is counted under a reason from a closed vocabulary, so a shortfall is always visible as a number somewhere rather than as an absence nobody can see. | `pdf_image_unreadable`, `pdf_inline_image` | an image neither read nor counted, or a reason outside the declared list |
| K6.8 | The office formats produce figures **with no renderer**: their pictures are stored as parts, and all four readers emit them under **one vocabulary held in one place**, with format-specific addressing in the **typed locator** rather than among the facts. A format that does not state a placement emits it as unknown rather than omitting it or guessing. | `docx_embedded_image`, `pptx_embedded_image`, `xlsx_embedded_image` | a per-format fact set, an addressing key smuggled into the facts, a renderer required to read a part, a fact dropped where a format is silent, or a figure record no build plan turns into a node |
| K6.9 | A picture an office reader cannot take whole **never costs the document it is in**. Where the bytes are reachable the figure is emitted with what could not be read left **unknown**; where they are not, it is a **counted skip** under one vocabulary the three readers share. An exception raised while describing one picture is not a reason to lose the rest of a document. | `docx_image_unreadable`, `pptx_image_unreadable` | a document that fails to read because one picture will not parse, a picture dropped without a count, a reason outside the shared vocabulary, or a dimension guessed where the header gave none |
| K6.10 | An image is a placement **wherever it is drawn**, including inside a Form XObject. The reader descends into forms to a declared depth, composing each form's `/Matrix` and the transformations around it, so a box is still the box the image reached the page with. A descent that will not terminate — a form deeper than the limit, a form that invokes itself — is a **counted skip**, never an unseen one: a bound that stops in silence is the same lost placement in a new place. | `pdf_image_in_form`, `pdf_form_pathologies` | an image drawn only inside a form going unemitted and uncounted, a box that ignores the form's matrix or the transformation that invoked it, or a depth limit or cycle guard that stops without counting |
| K6.11 | A caption binding is an **inference**: a new node, `origin=inferred`, confidence below 1, under the `caption-binding` channel, naming the rule that produced it and citing the paragraph it was read from. The figure and the words each keep their own provenance; neither is rewritten by the claim that they belong together. **Figures only.** | `pdf_caption_below` | a binding at confidence 1, one filed as an observation, one on a reader channel, or a binding to a table |
| K6.12 | Binding applies **ordered hard rules, each naming itself** — below-overlapping, below-offset, above-overlapping — and the first that binds is the one recorded, with **that rule's** declared confidence. Assignment is set-level: a line captions at most one figure, and candidates are taken best-first across the page rather than in the order figures happen to appear. | `pdf_caption_below` | an unordered test, a confidence not drawn from the rule that fired, a blended score, or one line captioning two figures |
| K6.13 | Where the evidence does not decide, the analyzer **abstains**, in a word from a closed vocabulary, carried by the figure it is about. Two candidates equally close on opposite sides are not settled by rule order — an order is a preference, not evidence. A figure whose only candidate went to a nearer figure says **that**, and is never reported as one nothing came near. | `pdf_caption_ambiguous` | a binding settled by tie-break, an abstention that is merely absence, a reason outside the vocabulary, or `candidate-already-bound` reported as `no-candidate-within-gap` |
| K6.14 | A region of vector drawing becomes a figure only with the **declared minimum of operators and of extent** behind it, and **every refusal is counted with its reason**. A rule line is the case this exists for: the commonest mark in a document, not a figure, and a rule that promoted it would fill every tree with furniture. | `pdf_vector_region`, `pdf_no_objects` | a rule line promoted, a refusal absent from the counts, or a page of prose reported as though nothing had looked |
| K6.15 | A rendered region is an **inference and says so** — `origin=inferred`, `source=render`, the `object-render` channel — and its **asset is the hash of the drawing operators and their extent**, never of the raster. The raster is a derived artefact cached under its own key of source, renderer, version and resolution, and is **no part of tree identity**: two renderers may disagree about pixels without disturbing K4. | `pdf_vector_region` | a rendered figure indistinguishable from an extracted one, an identity that changes when the renderer or its resolution does, or raster bytes reachable from a node |
| K6.16 | **Nothing in the default path reaches the AGPL renderer.** The guard refuses the import under this package's paths outside the single named adapter of the opt-in extra, **and** the module is absent from `sys.modules` after every default route has run. A licence obligation that depends on which code path executed is not a licence obligation anyone can audit. | `pdf_vector_region`, `pdf_no_objects` | an import under the scoped paths, or the module present in `sys.modules` after a default route has read and rendered these |
| K6.17 | **A region is a drawing, not a page.** Page furniture — a mark spanning the page in both dimensions, or a straight rule spanning it in one — is **excluded before grouping** and counted by reason, because a single background rectangle merged transitively reaches every mark on the page and turns the region into the MediaBox. A cluster that still spans the page after that is **refused**, also counted: the backstop is for the chains furniture does not explain. A page's own dimensions are read from the page, never assumed. | `pdf_page_furniture` | furniture merged into a region, a mark removed without a count, a region whose box is the page, or an extent measured against an assumed page size |
| K6.18 | A table that arrives as a **picture** is routed at its own declared confidence, under a rule that names itself, and is **never reported among the tables a reader saw**. A lattice of strokes is not a table: it has no cells to read, no header to find and no values to extract, and letting it into the table vocabulary would put un-navigable objects where downstream work expects readable ones. | `pdf_table_as_image`, `pdf_filled_cell_table` | a drawn lattice emitted as a `table` node, routed at the ordinary region confidence, routed under a rule that does not name it, or a table refused because its cells are filled rather than ruled |
| K6.19 | A page of ink with **no text layer** names the skip and carries what it declined to interpret — its pages and its objects. Reporting such a page as read and empty is the failure this exists to prevent: a scan with no characters and a page that genuinely holds none are indistinguishable in a count, and only one of them is a document nobody has read. | `pdf_ink_no_text` | a page without characters passing for one that was read, or a skip absent from the counts |

---


## K7 — the store: what was read survives the process that read it

Phase P7. A tree that exists only inside one run cannot be cited later, so the
store keeps documents, the objects and edges read from them, the chunks cut from
them, and the vectors computed over those chunks — in one SQLite file.

One file, deliberately. A vector store beside a document store is two things that
can disagree about what exists, and the disagreement is discovered at query time,
by a user.

Two identity rules carry the layer, and both say the same thing: a thing is what
it contains, never where it was found or when. `doc_id` is the source sha256;
`chunk_id` digests `(doc_id, level, node_ids, text)`. Neither holds a float, a
timestamp or a path.

| id | proposition | fixture | falsified by |
|---|---|---|---|
| K7.1 | A document is its bytes: `doc_id` is the source sha256, the same bytes at two paths are one document with two recorded paths, and an id built from anything else is refused at construction. | `trees_per_format` | one document becoming two because it moved, or an id accepted that is not a digest |
| K7.2 | A stored tree returns identical: the canonical JSON of a document read back equals the canonical JSON stored, for every format. Node addresses derived for the store resolve back to their nodes and are unique. | `trees_per_format` | a round trip that changes one byte, an address that does not resolve, or two nodes sharing one |
| K7.3 | No text a reader found is lost: every node carrying text is chunked or absorbed by a node that was, and every node that is neither is refused with a reason from a closed vocabulary. A chunk with no node cannot be constructed. | `slide_deck`, `mixed_workbook`, `docx_two_tier` | a read document producing no chunk, text reaching no chunk, or a node neither chunked nor refused |
| K7.4 | A level-1 roll-up covers exactly the union of its level-0 children's node ids, in order, and carries no parent of its own. Roll-ups can be switched off without affecting level 0. | `docx_two_tier` | a roll-up naming a node no child names, missing one a child does, or holding a parent |
| K7.5 | A chunk is its content: chunking an unchanged tree twice yields identical ids and the store writes nothing the second time. Every component of the id is load-bearing, node order included. | `trees_per_format` | an insert or a delete on re-chunking, or two different chunks sharing an id |
| K7.6 | Embeddings are keyed `(chunk_id, model)`: two models coexist, embedding again computes only what is missing, and a partial or empty answer from a backend is refused rather than stored. | `docx_two_tier` | an embedder asked twice for the same chunk, a model overwriting another, or a stored vector shorter than its declared dimension |
| K7.7 | `embedder.provider: none` writes nothing at all — not a zero vector — and the store still answers on its lexical lane, with `dense: disabled (no embedder)` stated rather than inferred. | `docx_two_tier` | any row in `embeddings` after indexing with no embedder |
| K7.8 | The dense lane reads its vectors from the database and the index is a rebuildable cache: dropping it and reopening reproduces identical hits, and changing a document's chunks discards it. | `docx_two_tier` | a hit list that changes when only the cache does, or a cache that outlives the rows it describes |
| K7.9 | A hit keeps its lane ranks beside the fused rank, never replaced by it: a hit found by one lane carries `None` for the other, and fusion is over ranks, never over lane scores. | `docx_two_tier` | a lane rank replaced by a fused one, or an absent lane reported as a worst-case number |
| K7.10 | Every hit resolves to provenance: the nodes a chunk names are reachable in the stored tree and each carries a non-empty citation chain naming its source and format. | `docx_two_tier` | a hit whose nodes do not resolve, or one resolving to a node citing nothing |
| K7.11 | Objects and edges read from a tree are stored and returned with the hits that cite their nodes. Edges are copied from what an analyzer decided and recorded; a binding whose target cannot be resolved is refused and counted, never guessed. | `pdf_caption_below` | an edge no analyzer decided, a caption bound to the line it was made from, or an unresolved binding dropped in silence |
| K7.12 | Delete is trash: the document is marked and its vectors parked, restore returns those same vectors component-wise, and purge is opt-in and counted in the store's drop trace. | `mixed_workbook` | a restore that recomputes rather than returns, a purge that reports nothing, or a trashed document still answering a query |
| K7.13 | The lexical lane is written with the chunk: a search after an upsert finds the new text, an empty query returns nothing rather than everything, and a trashed document leaves the lane. | `docx_two_tier` | a chunk searchable before it is stored, or a deleted document still returned |
| K7.14 | The packaged defaults are the shape: an unknown key is refused with its path, a value where a mapping belongs is refused, a missing file is refused rather than ignored, and an overlay changes what a real run does. | `docx_two_tier` | a typo accepted and the default used silently, or an overlay a run ignores |
| K7.15 | `saqqara_index` declares `requires=["document_tree"]` and `provides=["document_store"]`, the envelope enforces that declaration, and a store provider resolves without anyone having imported its module first. | `docx_two_tier`, `markdown_document` | a run proceeding with no document tree, or a provider that resolves only by import order |
| K7.16 | The CLI and the MCP surface return the same hits for the same query on the same store, each carrying both lane ranks and its provenance. | `docx_two_tier`, `markdown_document` | one surface answering differently from the other, or a hit from either lacking its citation |
| K7.17 | **Fusion beats the lexical lane alone.** On a corpus where a decoy wins the lexical lane, the fused ranking places the answer above it, and the lane rank it beat is asserted rather than assumed. A fusion that reproduced a lane would be indistinguishable from one that fused nothing, and no property of the formula can say otherwise — only a comparison can. | `fusion_decoys` | the answer ranking no better under fusion than under the lexical lane, or a fixture whose lexical lane does not in fact rank a decoy first |
| K7.18 | **Fusion beats the dense lane alone**, on the same corpus and by the same standard — where the decoy that wins the dense lane never uses the query's words, so the two lanes are wrong about different documents. | `fusion_decoys` | the answer ranking no better under fusion than under the dense lane, or a fixture whose dense lane does not in fact rank a decoy first |
| K7.19 | **A secret is a reference, resolved at use and never stored.** A configuration naming `env:VAR` or `file:/path#Label` serialises the reference and never the value, so a config object is always safe to write down; the value is read at the moment it is used. Every unresolvable form fails closed — an unset variable, an unreadable file, an empty one, an absent label, an unknown scheme, and a raw value with no scheme at all — because returning the reference would send the string `env:TOKEN` as a credential, which fails far away and unrecognisably. | `labelled_secret_file` | a loaded configuration whose serialisation contains the value, a reference resolved before use, or any unresolvable form returning instead of raising |

**Not in this phase, and declared rather than left open:** authority, freshness and
quality boosts (the `boosts` field exists and is empty — they need policies this
package does not own); store providers other than sqlite; OCR.


## Known holes — where an invariant stops holding

Not propositions: a proposition is a claim this package makes and defends with a fixture. These are
places where a claim is known to be **narrower than it reads**, recorded so that nobody rediscovers
them as surprises. Each names the boundary, what it costs, and the route by which it could be
closed. Ids are `H`-prefixed so they can never be read as gate propositions.

| id | where the invariant stops | cost | route to closing it |
|---|---|---|---|
| H1 | K6.7 and K6.9 promise that every picture is **read or counted**. That holds above the library boundary only. In the spreadsheet format, `openpyxl` discards an image it cannot decode **during load** — `UserWarning: The image xl/media/....png will be removed because it cannot be read` — so `worksheet._images` never contains it and no reader code runs. Both a corrupt part and an empty one vanish this way; measured, not inferred. | A spreadsheet can hold pictures this kernel neither emits nor counts, and the count cannot see its own blind spot. The two other office formats do not share it: both expose the bytes independently of parsing them. | Compare the drawing anchors declared in the sheet's own XML (`xl/drawings/drawing*.xml` and its relationships) against the images the library kept, and count the difference under the existing `PART_SKIPS` vocabulary. This reads the format directly rather than trusting the library's inventory, which is the same move `_boxes` already makes for the page description format. |
