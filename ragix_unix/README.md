# RAGIX Unix Tools

This directory contains the command-line interfaces (CLIs) for the RAGIX suite. Each file corresponds to a `ragix-*` command, providing a Unix-like interface to the platform's capabilities.